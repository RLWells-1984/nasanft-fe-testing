import { Platform } from "react-native";

import colors from "./colors";

export default {
  header: {
    fontSize: 26,
    color: colors.blue_text,
    fontWeight: "500",
  },
  text: {
    fontSize: 18,
    color: colors.blue_text,
  },
};
